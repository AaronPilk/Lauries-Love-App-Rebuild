import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    gap: 24,
  },
  lists: {
    gap: 12,
    paddingBottom: 40,
  },
});

export default styles;
