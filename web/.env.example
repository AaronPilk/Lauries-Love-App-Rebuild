# Copy to .env and fill from the mobile app's app/.env (same Supabase project).
# NOTE: Vite exposes only VITE_-prefixed vars to the browser. The anon key is a
# publishable key by design; real protection is the database's row-level rules.
VITE_SUPABASE_URL=https://iwbfsbriippzmdyrsmsu.supabase.co
VITE_SUPABASE_ANON_KEY=your-publishable-anon-key-here
