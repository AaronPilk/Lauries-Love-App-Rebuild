import { PermissionsAllType } from './PermissionsProvider.types';

export const PERMISSIONS_DEFAULT: PermissionsAllType = {
  imagePicker: null,
  camera: null,
  notificationFirebaseIOS: null,
  notificationFirebaseAndroid: null,
};
