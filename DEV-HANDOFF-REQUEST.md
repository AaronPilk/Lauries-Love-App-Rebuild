# Laurie's Love — Data & Asset Handoff Request

To: One Seven Tech
From: Aaron Pilkington (aaron@skyway.media), on behalf of Laurie's Love
Re: Read-only data exports + account transfers for platform transition

Everything below is **read-only** — nothing here requires changes to the
running production system, and the live app stays untouched throughout.
Please deliver via a secure channel (encrypted archive + separate password,
or a shared drive with restricted access). Timeline request: within 2 weeks.

---

## 1. DATABASE (MySQL)

- [ ] Full logical dump of the production MySQL database (`mysqldump
      --single-transaction`, all tables + schema), taken from a replica or
      during low traffic. Tables of record: `user`, `event`,
      `friend_request`, `notification` (+ object/change tables), `payment`,
      `values_definition`, `definitions_type`, migration history.
- [ ] Row counts per table at dump time (for our validation).

## 2. AUTH (AWS Cognito — user pool us-east-1_K3nWoTP66)

- [ ] Full user export CSV (Cognito "Export users" / ListUsers API dump):
      sub, email, email_verified, status, created date, all custom
      attributes.
- [ ] Confirmation of which attributes are marked verified.
- [ ] (We understand password hashes cannot be exported — users will set a
      new password on first login of the new platform. No action needed.)

## 3. CHAT + COMMUNITY FEED (Sendbird)

- [ ] Full data export via Sendbird's export API or dashboard: group
      channels (including channel `data`/metadata JSON — posts live there),
      messages, reactions, users, friend relationships.
- [ ] The application ID and region (for mapping exported URLs).
- [ ] Any media files hosted on Sendbird's CDN (message attachments).

## 4. MEDIA (AWS S3)

- [ ] Bucket name(s) + a full object listing (key, size, last-modified).
- [ ] Either: temporary read-only credentials/presigned access so we can
      copy objects, OR a bulk export (S3 sync to a drive we provide).
      Profile pictures and post images are the priority.

## 5. PAYMENTS (Authorize.Net) — donation continuity

- [ ] Confirmation of which Authorize.Net account owns the customer
      profiles (the org's account or an agency account).
- [ ] If the org's account: nothing to transfer — we need the API login ID
      regenerated by the org later, NOT from you.
- [ ] If an agency account: list of active recurring donation subscriptions
      (customer profile IDs are already in the user table config JSON) and a
      plan for migrating billing agreements to the org's own account.
- [ ] Any webhook endpoints currently configured.

## 6. APPLE APP STORE + GOOGLE PLAY (the big one)

- [ ] Initiate **App Store listing transfer** of "Laurie's Love"
      (com.SMv587dd8da82c.app) to the organization's / Aaron's Apple
      Developer account. (App Store Connect -> App -> App Information ->
      Transfer App.) This preserves all users, reviews, and update path.
- [ ] Same for the Google Play listing (Play Console -> transfer app).
- [ ] Push certificates/keys do NOT need to transfer (we issue new ones).

## 7. CONFIGURATION INVENTORY (answers, not credentials)

- [ ] Any scheduled jobs/crons beyond the nightly subscription check?
- [ ] Any webhooks (Sendbird, Authorize.Net, Firebase) pointing at the API?
- [ ] Deep link / universal link domains configured (apple-app-site-association)?
- [ ] Anything else that writes to the database besides the mobile app +
      the NestJS API (admin panels, scripts)?
- [ ] Expo/EAS project owner + whether OTA updates are still being published.

## 8. EXPLICIT NON-ASKS (to keep this clean)

We are NOT asking you to: modify production, rotate keys, share private
keys/secrets for running services, or take the app offline. Production
should keep running unchanged until a coordinated cutover, which we'll
schedule with the organization separately.

---

Questions to: aaron@skyway.media
